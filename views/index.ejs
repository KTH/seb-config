<%- include ("partials/header") %>

<div class="container pt-3">
    <form action="<%=process.env.PROXY_PATH_PREFIX%>/q" method="POST">
      <div class="form-group">
          <label for="courseID">Please provide KTH Canvas Course ID:</label>
          <input type="text" maxlength="10" pattern="\d*" title="numbers only" class="form-control col-md-2" id="courseID" name="courseID" placeholder="courseID" value="" required><small id="idHelpInline" class="text-muted">
            Numbers only, max 10 digits. (http://canvas.kth.se/courses/<b>courseID</b>)
          </small>
        </div>
        <button type="submit" class="btn btn-success" value=1 name="limit">Generate</button>
         
  </form>
</div>
<div class="container pt-3">
  <small class="text-muted">
  You can also send a post request to /q? with courseID=xxxx
</small>
</div>
<div class="container pt-3">
<form action="<%=process.env.PROXY_PATH_PREFIX%>/q" method="POST">
    <button type="submit" class="btn btn-info" value=0 name="limit">Generate Universal</button>  
</form>
</div>
<div class="container pt-3">
  <small class="text-muted">
  You can also send a post request to /q? with limit=0 for universal.
  Note: /q?courseID=xxx&limit=0 return Universal as well.
</small>
</div>

<%- include ("partials/footer") %>


